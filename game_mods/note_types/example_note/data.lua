-- this gets called starts when the note loads.
function onNoteSpawn(noteType) -- arguments, the song name

end

-- this gets called starts when the note loads.
function onNoteMiss(misses) -- arguments, the song name
    health = 5
end

